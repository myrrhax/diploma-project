spring:
  application:
    name: diploma-project

  datasource:
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: validate
      dialect: org.hibernate.dialect.PostgresSQLDialect

  liquibase:
    change-log: classpath:liquibase-master.yaml

  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}
    port: 5672

app:
  jwt:
    secret: ${APP_JWT_SECRET:somesupersecterkeyANSFKJA@!NFLSKanfdOIB)!@BFKABSfkjsaFaSF}
    issuer: myrrermdev.space
    accessTokenTtl: 15m
    refreshTokenTtl: 30d

  security:
    refresh-cookie-name: Refresh-Token
    refresh-cookie-security: false
    confirmation-code-duration: 1d

  rabbit:
    send-mail-queue: mails

  cache:
    schema-ttl: 15m

server:
  port: 8000

logging:
  level:
    liquibase: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.tool.schema: DEBUG